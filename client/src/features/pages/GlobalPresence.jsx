import PageShell from "../../shared/layouts/PageShell/PageShell";

export default function GlobalPresence() {
  return (
    <main className="page">
      <div className="container">
        <p className="page-eyebrow">Hiện diện toàn cầu</p>
        <p className="page-desc">Đây là trang Hiện diện toàn cầu</p>
      </div>
    </main>
  );
}
